# Claude Code restart prompt

Paste the block below into a fresh Claude Code session (in `/Users/darrell/Zarathustra`) to restore the race-mode cron loop.

---

Schedule three recurring crons (use CronCreate three times):

1. cron "7 */2 * * *", recurring true, prompt:
"Read PEER-REVIEW.md and PEER-REVIEW-GEMINI.md and check if there are any new peer review comments, action items, or updates since last check. Report on anything that needs attention. PEER-REVIEW.md is Darrell's, PEER-REVIEW-GEMINI.md is Gemini's."

2. cron "3,33 * * * *", recurring true, prompt:
"Check the status of ALL training runs currently active on vinge.local (check `ps aux | grep train.py` and read the latest log lines). Report progress. ATBs: alibaba=0.176, tencent=0.178 (frozen-bundle, seed=42). Kill threshold: 30 epochs stale from best star (per memory rule, overrides any older value). W-stop: 3.0. If a run is hopeless to BEAT the current best, kill it and launch the next idea from IDEAS.md. Keep 2 concurrent runs (1 alibaba, 1 tencent). Update VERSIONS.md with post-mortems. ALWAYS commit AND PUSH after changes (push is required — peer-review pipeline reads remote). We are in a race — move fast."

3. cron "*/30 * * * *", recurring true, prompt:
"Check the status and report on it; if it looks like it is hopeless for beating the current best, kill is and move on; we have a long to-do list of ideas to try. work through those ideas sequentially and autonomously. We are in a race with another team."

Then sweep status: ssh to vinge.local, ps aux | grep train.py, tail recent logs of any active runs, and report.
